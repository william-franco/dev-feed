name: dev_feed_app
description: "Dev Feed — feed de posts com autenticação JWT."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter

  connectivity_plus: ^7.1.1
  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  get_it: ^9.2.1
  go_router: ^17.2.3
  location: ^8.0.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  mockito: ^5.6.4

flutter:
  uses-material-design: true
